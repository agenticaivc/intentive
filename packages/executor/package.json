{
  "name": "@intentive/executor",
  "version": "0.1.0",
  "description": "Intentive graph execution engine",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "test:watch": "jest --watch",
    "dev": "tsc --watch"
  },
  "keywords": [
    "intentive",
    "executor",
    "graph",
    "workflow"
  ],
  "peerDependencies": {
    "typescript": "^5.8.3"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "jest": "^29.7.0",
    "ts-jest": "^29.3.4"
  },
  "engines": {
    "node": ">=18.0.0"
  }
} 